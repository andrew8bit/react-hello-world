// Class based Component
import React, { Component } from 'react';

class Car extends Component {
    render() { // we need the render method
        return (
            <div>
                <h1>Car</h1>
            </div>
        );
    }
}

export default Car; 